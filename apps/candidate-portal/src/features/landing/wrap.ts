/** The landing's shared content column: centred, capped at 1120px, 28px side gutters. */
export const WRAP = 'mx-auto w-full max-w-[1120px] px-7';

// The employer explainer is the Recruiter Portal's public landing (its own ticket, its own app).
// No client env carries its host yet, so this is a documented placeholder until hosting lands.
export const RECRUITER_PORTAL_URL = 'https://recruiters.sync.ngo';
