# Copy to `.env.local`. Empty means same-origin: requests go through Vite's dev proxy so the
# HttpOnly session cookies stay same-site. Only set this to an absolute URL when the portal is
# served from a different origin than the API.
VITE_API_BASE_URL=
