[project]
name = "sync-parsers"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "super", email = "moofk2002@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    # The Files API and the Responses API's structured outputs, both of which ADR-0006
    # depends on. Confined to this package: the port is what everything else imports.
    "openai>=2.48.0",
    "sync-core",
]

[build-system]
requires = ["uv_build>=0.11.24,<0.12.0"]
build-backend = "uv_build"

[tool.uv.sources]
sync-core = { workspace = true }
