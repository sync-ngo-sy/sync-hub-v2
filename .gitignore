# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.turbo/
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ruff_cache/
.pytest_cache/
.mypy_cache/
*.egg-info/

# Env
.env
.env.*
!.env.example
!.env.compose.example

# Editor / OS
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
.DS_Store

# Coverage / logs
coverage/
*.log

# Supabase
supabase/.branches/
supabase/.temp/

# Note: generated types (api-client schema, SQLAlchemy models) are committed on
# purpose so the repo type-checks without a running backend.
.superpowers/
.omo/
.worktrees/

# Reachability probe output — contains the tester public IP; never commit (repo is public)
reachability-report*.txt

# Firebase CLI deploy cache, written next to firebase.json on every deploy.
.firebase/

# Graphify knowledge-graph output
graphify-out/

# TanStack Router's scratch space, where it stages routeTree.gen.ts before renaming it into place.
.tanstack/
